PORT=3001
VITE_BACKEND_URL=http://localhost:3001

# Canonical public URL of the deployed site, no trailing slash. Drives the
# share card, canonical link, sitemap and structured data. Leave unset on
# Vercel or Netlify: the platform's own build variables are detected.
VITE_SITE_URL=https://your-site.vercel.app
